{
  "name": "ai-sales-coach-backend",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run migrate && bun run auth:migrate && bun --watch src/index.ts",
    "start": "bun run migrate && bun run auth:migrate && bun src/index.ts",
    "migrate": "bun scripts/migrate.ts",
    "auth:migrate": "bun scripts/auth-migrate.ts",
    "seed": "bun scripts/seed.ts",
    "seed:cues": "bun scripts/seed-cue-framework.ts",
    "analyze:fixture": "bun scripts/analyze-fixture.ts",
    "ingest:smoke": "bun scripts/ingest-smoke.ts",
    "demo:data": "bun scripts/demo-data.ts",
    "test": "bun test",
    "typecheck": "bunx tsc --noEmit"
  },
  "dependencies": {
    "better-auth": "^1.6.24",
    "express": "^4.19.2",
    "pg": "^8.12.0",
    "posthog-node": "^5.46.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.0",
    "@types/pg": "^8.11.0",
    "typescript": "^5.5.0"
  }
}
